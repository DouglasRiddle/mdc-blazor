@namespace SsExample.Pages.Examples

<ParameterSection>
    <div>
        Expected Layout<br /><pre><code>@(@"<MdcTable>
    <TableDefinition>
        <MdcTableRow>
            <MdcTableColumn />
        </MdcTableRow>
    </TableDefinition>
    <NoResultsMessage>Optional</NoResultsMessage>
</MdcTable>")</code></pre>
    </div>

    <div>
        Universal Parameters<br />
        <strong>Class</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to pass CSS class names
    </div>
    <div><strong>Style</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to pass inline-CSS</div>

    <MdcDivider />

    <div>
        <code>MdcTable</code> Parameters<br />
        <div><strong>Id</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to pass component id string</div>
    </div>
    <strong>InitialSortColumn</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to set the column the table will sort by on render. This should match the property name exactly.
    <div><strong>InitialSortOrder</strong> (<code>MdcTableColumnOrderBy</code>)<br /><em>Optional</em><br />Used to set the sort order of the InitialSortColumn on render.<br />Values: <code>MdcTableColumnOrderBy.ascending</code> (default), <code>descending</code></div>
    <div><strong>Items</strong> (<code>@("List<T>")</code>)<br /><strong>Required</strong><br />List of items to display</div>
    <div><strong>Label</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to set the Table label.</div>
    <div><strong>NoResultsMessage</strong> (<code>RenderFragment</code>)<br /><em>Optional</em><br />Text to show when the list of items count is zero.</div>
    <div><strong>PageLabel</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to set the results page drop-down label. Default is "Rows per page:"</div>
    <div><strong>PageSize</strong> (<code>MdcTablePageSize</code>)<br /><em>Optional</em><br />Used to set the default number of items per paget<br />Values: <code>MdcTablePageSize._5</code>, <code>_10</code> (default), <code>_25</code>, <code>_50</code>, <code>_100</code>, <code>_500</code>, <code>_1000</code></div>
    <div><strong>Selectable</strong> (<code>bool</code>)<br /><em>Optional</em><br />Used to set the selectability of table rows.</div>
    <div><strong>ShowPagination</strong> (<code>MdcTablePaginationDisplay</code>)<br /><em>Optional</em><br />Used to set the visibility of the pagination section<br />Values: <code>MdcTablePaginationDisplay.always</code> (default), <code>autohide</code>, <code>never</code></div>

    <MdcDivider />

    <div>
        <code>MdcTableRow</code> Parameters<br />
        <strong>OnClick</strong> (<code>@("EventCallback<MouseEventArgs>")</code>)<br /><em>Optional</em><br />Used to pass parameter to run on TableRow click. Expected to be used in conjunction with <code>MdcTable Selectable = true</code>
    </div>

    <MdcDivider />

    <div>
        <code>MdcTableColumn</code> Parameters<br />
        <strong>HeaderText</strong> (<code>string</code>)<br /><strong>Required</strong><br />Used to set column header
    </div>
    <div><strong>IsNumeric</strong> (<code>bool</code>)<br /><em>Optional</em><br />True value sets special formatting for numbers</div>
    <div><strong>OnClick</strong> (<code>@("EventCallback<MouseEventArgs>")</code>)<br /><em>Optional</em><br />Used to pass parameter to run on TableColumn click.</div>
    <div><strong>SortColumn</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to specify the property to sort by when this column header is clicked. This should match the property name exactly.</div>
    <div><strong>Title</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to specify tooltip text displayed on hover.</div>
    <div><strong>UniqueKey</strong> (<code>string</code>)<br /><em>Optional</em><br />Required if a unique value is not generated by the combination of the HeaderText, IsNumeric and SortColumn parameters. Must be unique within each <code>MdcTableRow</code> section.</div>
</ParameterSection>
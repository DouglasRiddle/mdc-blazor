@namespace SsExample.Pages.Examples

<ParameterSection>
    <div>
        Expected Layout<br /><pre><code>@(@"<MdcDialog>
    <MdcDialogSurface>
        <Content>Html Content</Content>
        <Actions>
            <MdcDialogActions>
                <MdcDialogButton />
            </MdcDialogActions>
        </Actions>
    </MdcDialogSurface>
</MdcDialog>")</code></pre>
    </div>

    <div>
        Universal Parameters<br />
        <strong>Class</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to pass CSS class names
    </div>
    <div><strong>Id</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to pass component id string</div>
    <div><strong>Style</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to pass inline-CSS</div>

    <MdcDivider />

    <div>
        <code>MdcDialog</code> Parameters<br />
        <strong>EscapeKeyAction</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to set the action when Escape is pressed while a dialog is open. Set <code>EscapeKeyAction=""</code> for no action. Default action is <code>close</code>
    </div>
    <div><strong>IsFullScreen</strong> (<code>bool</code>)<br /><em>Optional</em><br />A <code>true</code> value indicates the dialog should be full-screen on mobile. On desktop this remains modal, but with a close button.</div>
    <div><strong>ScrimClickAction</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to set the action when the scrim is clicked while a dialog is open. Set <code>ScrimClickAction=""</code> for no action. Default action is <code>close</code></div>
    <div><strong>TimeOut</strong> (<code>TimeSpan</code>)<br /><em>Optional</em><br />Used to set the amount of time a tracked dialog will wait for a choice. Defaults to 100 seconds. Pass as <code>TimeSpan.FromSeconds(100)</code></div>
    <div><strong>Title</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to set the header title</div>

    <MdcDivider />

    <div>
        <code>MdcDialogButton</code> Parameters<br />
        <strong>ButtonStyle</strong> (<code>MdcButtonStyle</code>)<br /><em>Optional</em><br />Values: <code>MdcButtonStyle.flat</code> (default), <code>outlined</code>, <code>raised</code>, <code>unelevated</code><br />Used to set button style.
    </div>
    <div><strong>DefaultAction</strong> (<code>bool</code>)<br /><em>Optional</em><br />True value sets this button as the action to be taken when "Enter" is pressed within dialog. Should only be used once per dialog.</div>
    <div><strong>DialogAction</strong> (<code>string</code>)<br /><em>Optional</em><br />The returned value for a tracked dialog on click</div>
    <div><strong>Disabled</strong> (<code>bool</code>)<br /><em>Optional</em><br />Used to set button enabled/disabled state</div>
    <div><strong>Icon</strong> (<code>string</code>)<br /><em>Optional</em><br />Add <a href="https://material.io/resources/icons/?style=baseline" target="_blank">Material Icon</a> to button</div>
    <div><strong>IsInitialDialogFocus</strong> (<code>bool</code>)<br /><em>Optional</em><br />True value indicates this element should be focused on dialog open.</div>
    <div><strong>Label</strong> (<code>string</code>)<br /><em>Optional</em><br />Used to set button text</div>
    <div><strong>OnClick</strong> (<code>@("EventCallback<MouseEventArgs>")</code>)<br /><em>Optional</em><br />Used to pass parameter to run on button click</div>
    <div><strong>Type</strong> (<code>MdcButtonType</code>)<br /><em>Optional</em><br />Values: <code>MdcButtonType.button</code> (default), <code>submit</code><br />Used to set button type.</div>
</ParameterSection>